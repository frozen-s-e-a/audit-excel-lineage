# 项目开发约定

## 目标

在用户自己的电脑批量提取 Excel 计算结构，生成可复核的脱敏逻辑包。先阅读 README 和 docs 内规范。当前 0.4 为仅数值处理基础程序；完整溯源仍是设计。以 README、docs/PRIVACY_SPEC.md 和 docs/QUICK_START.md 为当前实现依据，不将后续方案描述为已完成功能。

## 数据边界

- 不将真实审计文件、名称对照表、缓存、运行数据库、原始错误或真实单元格值提交到 GitHub、在线 CI 或外部模型。
- 测试数据从零虚构，不使用只改了文件名的真实底稿。
- 输入与运行目录位于代码仓库之外；原工作簿只读。
- 0.4 按用户授权复制工作簿并仅改数值/清理数值缓存，公式、文本及附属对象保留，必须标明不是完整匿名化。未来安全逻辑包另用字段白名单和独立安全类型。
- 网络模型连接、宏执行、外链刷新不属于第一版。

## 技术路线

当前数值处理使用 C#/.NET 10 + WPF + System.IO.Compression/XML，逐单元格流式处理，无 SQLite 和公式解析。Open XML SDK、SQLite、CLI 及依赖图属于后续完整方案。

## 实现要求

- 读取、解析、溯源、语义、脱敏、导出、存储和界面分层，核心引擎不依赖 GUI。
- 对跨文件歧义、动态引用、循环和手工值保留明确状态，不静默猜测。
- 保留函数参数次序、相对/绝对引用及支持范围内的原计算结构。
- 只对真实风险写测试，重点验证引用正确性、脱敏漏出和原文件未被修改。
- 依赖更新先在 Windows 验证后锁定；exe 需要在 Windows 构建并实测。
- 每次报告已完成项、实际验证命令、未支持项和下一步，不能用计划替代结果。

## 协作

新功能依据 docs/ROADMAP.md 的任务顺序推进。不要为满足任务修改或削弱隐私要求；冲突应说明具体影响，并优先完成不受影响的工作。
